import findmax
from findmax import find_max

numbers = [10, 5, 4, 3, 11]

print(findmax.find_max(numbers))
# import findmax

print(find_max(numbers))
# from findmax import find_max

# NOTE: if you give a variable a name that is already being used as a function, it will overwrite the function and you won't be able to use that function anymore.


